jenkins:
  globalNodeProperties:
    - envVars:
        env:
          - key: SOME_CASC_ENV_VAR
            value: a value configured via JCasC
unclassified:
  globalLibraries:
    libraries:
      - defaultVersion: "0.1"
        name: "test-functions"
        implicit: true
        retriever:
          legacySCM:
            scm:
              git:
                userRemoteConfigs:
                  - url: "https://github.com/apgsga-it/jenkins-pipeline-testlib.git"
                branches:
                  - name: "master"